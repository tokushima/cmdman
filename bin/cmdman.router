
if [ -n "$1" ]; then
	SRVHOST=$1
else
	SRVHOST=localhost:8080
fi

SS=$0

if [ -n $(readlink $SS) ]; then
	SS=$(pwd)/$(dirname $0)/$(readlink $SS)
fi
 
/usr/bin/env php -S  $SRVHOST $(dirname $(dirname $SS))/cmdman.router.php

